services:
  x:
    image: redis
    x-defang-redis:
      allow-downtime: true
      retention:
        final-snapshot-name: final-snapshot
        retention-period: 30d
        snapshot-to-load-on-startup: old-snapshot
  y:
    image: example
    x-defang-redis:

  z:
    image: redis
