services:
  x:
    image: postgres
    x-defang-postgres:
      allow-downtime: true
      retention:
        final-snapshot-name: final-snapshot
        retention-period: 30d
        snapshot-to-load-on-startup: old-snapshot
  y:
    image: example
    x-defang-postgres:

  z:
    image: postgres
