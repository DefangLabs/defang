services:
  no-dockerfile:
    build: .
  one-image:
    build:
      context: one_image
      dockerfile: Dockerfile
  mutiple-images:
    build:
      context: mutiple_images
      dockerfile: Dockerfile
