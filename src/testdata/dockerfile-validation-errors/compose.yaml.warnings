 ! project name "dockerfile-validation-errors" is longer than 16 characters, you may run into issues with resource name length
 ! service "invalid-dockerfile": ingress port 8080 without healthcheck; defaults to GET / HTTP/1.1; add a healthcheck such as: test: ["CMD", "curl", "-f", "http://localhost:8080/"]
 ! service "invalid-dockerfile": missing memory reservation; using provider-specific defaults. Specify deploy.resources.reservations.memory to avoid out-of-memory errors; suggest deploy.resources.reservations.memory: 512M
 ! service "invalid-dockerfile": service name is longer than 16 characters, you may run into issues with resource name length
