services:
  configdetection: 
    image: alpine
    environment:
      - API_KEY=50m34p1k3y # keyword detector
      - HIGH_ENTROPY_STRING=1234567890abcdef # high entropy string detector
      - MY_URL=https://user:p455w0rd@example.com # URL with password detector
      