 ! "yes" for boolean is not supported by YAML 1.2, please use `true`
 ! project name "compose-go-warning" is longer than 16 characters, you may run into issues with resource name length
 ! service "echo": ingress port 80 without healthcheck; defaults to GET / HTTP/1.1; add a healthcheck such as: test: ["CMD", "curl", "-f", "http://localhost:80/"]
 ! service "echo": missing memory reservation; using provider-specific defaults. Specify deploy.resources.reservations.memory to avoid out-of-memory errors; suggest deploy.resources.reservations.memory: 512M
