services:
  service1:
    image: nginx
    environment:
      PLAIN_VAR: plain_value
      SECRET_KEY: ${SECRET_KEY}
      PUBLIC_URL: https://example-${SECRET_KEY}-${USE_ENV}.com
      FROM_ENV: ${USE_ENV}
